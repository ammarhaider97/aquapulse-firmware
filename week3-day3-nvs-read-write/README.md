# Day 3 — NVS Calibration Storage
Adds NVS (Non-Volatile Storage) read/write for all five sensor calibration
coefficient sets, stored as a single blob. Falls back to factory defaults
when no saved data exists.
